// Base URL for API requests
// This constant defines the root endpoint for all API calls. 
// It is typically used as a prefix for specific endpoints in the app.
export const BASE_URL = '/api';

// Delay between polling requests in milliseconds
// This constant sets the interval duration (in milliseconds) between each repeated action (e.g., API polling). 
// It is used to control the frequency of API calls or other recurring tasks.
export const PING_DELAY = 5000; // 5 seconds
